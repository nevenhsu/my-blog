export type SpaceData = {
  height?: {
    base: number | undefined
    xs: number | undefined
    sm: number | undefined
    md: number | undefined
    lg: number | undefined
    xl: number | undefined
  }
}
