export type ColorData = {
  alpha: number
  hex: string
  hsl: { h: number; l: number; a: number; s: number }
  hsv: { h: number; a: number; s: number; v: number }
  rgb: { a: number; b: number; r: number; g: number }
}
